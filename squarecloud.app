# Nome que aparecerá no seu dashboard da Square Cloud
DISPLAY_NAME=Compila Judge API

# Arquivo principal para o deploy. Como temos um Dockerfile, usamos ele.
MAIN=Dockerfile

# Memória alocada para a aplicação (em MB). 512MB é um bom começo.
MEMORY=512

# Versão do Docker a ser usada. 'latest' é geralmente a melhor opção.
VERSION=latest
